my overly complex portfolio

- root contains the entire frontend built on astro 7, react, typescript.
- ai agent backend is built on fastapi, microsoft agent framework, and microsoft foundry. using azure ai search and cosmosdb for vector database storage and retrieval.
- impeccable design system is used for giving agents a consistent design system to work with
- ci/cd automates portfolio deployments with cloudflare pages and updates vector database indexes with upserts on changed content only.
- cv contains, as you would expect, my cv/resume. but also for some reason my certs (Microsoft Learn) and thomas more non-sensitive credentials.

i treat my portfolio as a learning sandbox sometimes so things may be messy
